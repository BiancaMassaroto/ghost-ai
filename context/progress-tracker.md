# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Foundation — design system and UI primitives, now with authentication wired in

## Current Goal

- `09-share-dialog.md` is complete: the navbar Share button opens a real Share dialog backed by collaborator APIs (list/invite/remove), enforcing ownership server-side and enriching collaborator emails with Clerk display name/avatar. Next up is the real canvas subsystem.

## Completed

- `01-design-system.md`: shadcn/ui initialized (`components.json`, base-nova/Base UI preset); Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea added to `components/ui/`; `lucide-react` installed; `lib/utils.ts` created with `cn()`; dark-only palette from `context/ui-context.md` wired into `app/globals.css` (mapped onto shadcn's semantic tokens and onto project utility tokens — `bg-base`, `text-copy-primary`, `border-surface-border`, `text-brand`, `bg-accent-dim`, etc.); `<html>` carries a permanent `dark` class since the app has no light mode. Verified: `tsc --noEmit`, `next build`, and `eslint` all pass clean; no `oklch` light-theme values remain in `globals.css`.
- `02-editor.md`:
  - `components/editor/editor-navbar.tsx` — fixed `h-14` top navbar (client component), dark `bg-surface` with `border-b border-surface-border`; left/center/right sections; left section holds the sidebar toggle `Button`, swapping `PanelLeftOpen`/`PanelLeftClose` off an `isSidebarOpen` prop; toggling itself is delegated to the caller via `onToggleSidebar` so navbar and sidebar can share one piece of state later; center and right sections are empty placeholders for now.
  - `components/editor/project-sidebar.tsx` — `fixed` overlay (`top-14 bottom-0 left-0`, `z-40`) so it floats above the canvas and never pushes layout; `isOpen` prop drives a `translate-x-0` / `-translate-x-full` slide transition; header with "Projects" title + close button; shadcn `Tabs` with "My Projects" / "Shared", each rendering the same empty-placeholder component; full-width `New Project` button with a `Plus` icon pinned to the bottom.
  - Dialog pattern: no new file added. `components/ui/dialog.tsx` (from `01-design-system.md`) already exposes `DialogTitle`, `DialogDescription`, and `DialogFooter` and already styles through the token-mapped shadcn semantic vars (`--popover`, `--popover-foreground`, `--muted`) from `globals.css`, so it already satisfies "title + description + footer actions, styled from tokens." Per the spec ("do not build actual dialogs yet") and the protected-foundation rule in `ai-workflow-rules.md`, it was left unmodified — no feature dialog (e.g. "New Project") was built.
  - Scope intentionally excludes wiring these two components into a shared layout or route — deferred per explicit instruction.
  - Verified: `tsc --noEmit` (via `next build`, after clearing stale `.next` typegen), `eslint`, and `next build` all pass clean on the new components.
- `03-auth.md`: Clerk wired into the app end to end.
  - Installed `@clerk/ui` (peer of the already-installed `@clerk/nextjs@7.8.3`).
  - `proxy.ts` (project root) — this Next.js 16 app uses `proxy.ts`, not `middleware.ts` (Next 16 renamed the convention; functionality is unchanged). Wraps `clerkMiddleware` from `@clerk/nextjs/server`; public routes are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` via `createRouteMatcher`, everything else calls `auth.protect()`. Note: `createRouteMatcher`/middleware-based protection is marked `@deprecated` in this Clerk version in favor of per-route `auth.protect()` calls, but the spec explicitly calls for the public-routes-in-middleware pattern, so that's what's implemented.
  - Added `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` to `.env.local` — these are Clerk's own well-known env var names (read directly by `@clerk/nextjs` internals), not invented ones; they didn't exist in `.env.local` yet so were added per the spec's "define public routes using the existing sign-in/sign-up env vars."
  - `lib/clerk-appearance.ts` — exports `clerkAppearance`, passed to `ClerkProvider`. Uses `@clerk/ui`'s `dark` theme (`{ theme: dark }` — note this Clerk version renamed the old `baseTheme` prop to `theme`) as the base, with `variables` overridden using the project's CSS custom properties from `globals.css` (`var(--accent-primary)`, `var(--bg-surface)`, etc.) instead of hardcoded colors, per spec and `code-standards.md`.
  - `app/layout.tsx` — wraps `<html>` with `ClerkProvider appearance={clerkAppearance}`.
  - `app/(auth)/layout.tsx` — shared two-panel (50/50) layout for auth pages (route group, doesn't affect URLs): left panel on `bg-surface` (vs. the right panel's `bg-base`) to differentiate it from the page background per the user's follow-up screenshot, holding a logo mark, a headline + supporting paragraph, an icon-badge feature list (`Sparkles`/`Share2`/`FileText` from `lucide-react`, each in a `bg-accent-dim`/`text-brand` badge), and a copyright footer pinned to the bottom via `justify-between`; hidden below `lg`. Right panel stays a centered, unstyled Clerk form column on `bg-base`. Copy matches the actual features in `project-overview.md`, not invented. This deviates from `03-auth.md`'s literal "text-only feature list" line — done at the user's explicit direction after they shared a target screenshot; no gradients, hero sections, boxed/bordered feature cards, or scroll-heavy layout, so the rest of the spec's constraints still hold.
  - `app/(auth)/sign-in/[[...sign-in]]/page.tsx` and `app/(auth)/sign-up/[[...sign-up]]/page.tsx` — Clerk's `<SignIn />`/`<SignUp />` components, unstyled beyond the global `appearance`; Clerk's own user menu/profile flows are left untouched.
  - `app/page.tsx` — server component; `auth()` from `@clerk/nextjs/server` redirects authenticated users to `/editor`, unauthenticated to `/sign-in`. (`/` is also protected by default in `proxy.ts`, so unauthenticated requests are actually redirected there first — this component's own unauthenticated branch is a defensive fallback, not the primary path.)
  - `components/editor/editor-navbar.tsx` — added Clerk's `<UserButton />` to the right section; no custom styling, inherits the global `appearance`.
  - Verified: `tsc --noEmit`, `eslint`, and `next build` all pass clean; manually smoke-tested with `next dev` — unauthenticated `GET /` returns a 307 to `/sign-in` (confirmed via raw headers, not just `-L`), and both `/sign-in` and `/sign-up` render 200 with the two-panel layout and the CSS-variable-driven appearance overrides present in the response HTML.
- `/editor` route: user hit a real 404 post-login because the route never existed (flagged as an open gap right after `03-auth.md` above). Fixed minimally — no feature-spec file covers this yet, so scope was kept to exactly the deferred wiring step already called out in `02-editor.md`, nothing more:
  - `components/editor/editor-shell.tsx` (new, client component) — lifts `isSidebarOpen` state (defaults `true`) and composes `EditorNavbar` + `ProjectSidebar`. Below the navbar, a `relative flex-1` region hosts the (still-`fixed`-overlay) `ProjectSidebar` plus a plain centered "Canvas workspace coming soon." placeholder — the real collaborative canvas (Liveblocks + React Flow, per `architecture-context.md`) isn't built yet, so nothing beyond an empty-state placeholder was added, consistent with the existing empty-state pattern already in `ProjectSidebar`'s tabs.
  - `app/editor/page.tsx` (new, server component) — just renders `<EditorShell />`; protected by `proxy.ts`'s default-protect rule like every other non-auth route.
  - Verified: `next build` now lists `○ /editor` as a real prerendered route (previously nonexistent); `eslint` clean; manually confirmed via `next dev` that unauthenticated `GET /editor` now 307s to `/sign-in?redirect_url=...` instead of 404ing.
- `04-project-dialogs.md`: editor home content, the three project dialogs, and sidebar project actions — all against mock data, no API calls or persistence.
  - `types/project.ts` (new) — `Project` interface (`id`, `name`, `slug`, `role: "owner" | "collaborator"`).
  - `lib/mock-projects.ts` (new) — seed `MOCK_PROJECTS` array (two owned, one collaborator) used until real project storage exists.
  - `lib/utils.ts` — added `slugify()` alongside `cn()`: lowercases, replaces non-alphanumeric runs with `-`, trims leading/trailing `-`.
  - `hooks/use-mock-projects.ts` (new) — owns the in-memory project list; `addProject`/`renameProject`/`deleteProject` mutate local state only (no backend), each auto-deriving `slug` via `slugify()`.
  - `hooks/use-project-dialogs.ts` (new) — the dedicated hook called for in the spec. Owns dialog mode (`"create" | "rename" | "delete" | null`), the active project being acted on, the name form field (plus a derived live `slug` preview), and `isSubmitting`. `openCreateDialog`/`openRenameDialog`/`openDeleteDialog`/`closeDialog` manage dialog state; `submitCreate`/`submitRename`/`submitDelete` each set `isSubmitting`, await a short artificial delay (300ms, no network — purely so the loading state in the UI is observable), call the corresponding data callback (from `useMockProjects`), then close. Deliberately decoupled from `useMockProjects` (takes `onCreate`/`onRename`/`onDelete` callbacks) so the dialog/form/loading concern stays separate from the mock-data-storage concern, per "keep modules small and single-purpose" in `code-standards.md`.
  - `components/editor/project-dialogs.tsx` (new, client) — Create/Rename/Delete dialogs built on the existing `components/ui/dialog.tsx` (unmodified, per the protected-foundation rule). Each `Dialog` is controlled via `open={mode === "..."}` / `onOpenChange` off `useProjectDialogs`'s state (confirmed `@base-ui/react/dialog`'s `DialogRoot.Props` — `open`/`onOpenChange` — via its `.d.ts` before using it, per `AGENTS.md`'s "check node_modules docs first" for this pre-release stack). Create: name input + live slug preview text under it. Rename: prefilled input, current name in the description, `autoFocus`, wrapped in a `<form onSubmit>` so Enter submits. Delete: no input, description-only confirmation, confirm button uses the existing `destructive` Button variant.
  - `components/editor/editor-home.tsx` (new, client) — the center-of-canvas empty state: heading, description, `New Project` button (`Plus` icon), no cards, per spec. Takes `onNewProject` and renders in place of the old "Canvas workspace coming soon." placeholder.
  - `components/editor/project-sidebar.tsx` — now renders real rows from mock data instead of always showing the empty state (empty state still shows when a list is genuinely empty). "My Projects" renders owned projects with hover-revealed Rename (`Pencil`)/Delete (`Trash2`) icon buttons; "Shared" renders collaborator projects with no actions, per spec. Added a `md:hidden` fixed backdrop scrim (`bg-black/50`) rendered while `isOpen`, clicking it calls `onClose` — satisfies "tapping outside the sidebar closes it" + "add a backdrop scrim" on mobile without changing desktop behavior (backdrop hidden at `md` and above). The existing bottom `New Project` button is now wired to `onNewProject`.
  - `components/editor/editor-shell.tsx` — composes `useMockProjects()` + `useProjectDialogs()`, wires: editor-home `New Project` → create dialog, sidebar `New Project` → create dialog, sidebar row rename/delete → rename/delete dialogs (all via the same `dialogs` object), and renders `<ProjectDialogs dialogs={dialogs} />` once at the shell level.
  - No new shadcn components added — sidebar row actions use plain `Button`s (existing `icon-xs` size) rather than introducing a dropdown-menu component, keeping scope to exactly what the spec asked for.
  - Verified: `tsc --noEmit`, `eslint` (0 errors — the one warning present is pre-existing, in an unrelated `.agents/skills/` template file), and `next build` all pass clean; `next dev` smoke-tested that `/editor` still 307s to sign-in when unauthenticated (chrome renders, nothing crashed).
  - Follow-up visual pass, from user-supplied reference screenshots: `editor-home.tsx` now centers via `absolute inset-0 flex items-center justify-center` instead of `h-full`, so it's pinned dead-center of the canvas area regardless of ancestor height-resolution; description-level text (`DialogDescription` in all three dialogs, `EditorHome`'s paragraph, sidebar empty-state labels) switched from `text-copy-muted` (`#808090`) to the lighter `text-copy-secondary` (`#c0c0cc`) for better contrast on the near-black background — done via `className` overrides on call sites, not by editing `components/ui/dialog.tsx`, per the protected-foundation rule. Also aligned Create-dialog copy with the reference: title "New Project", description "Give your project a name to get started.", secondary button relabeled "Cancel" → "Close" (all three dialogs, for consistency), and the slug preview line now renders only once the user has typed a name instead of showing a permanent "your-project-slug" placeholder.

- `05-prisma.md`: relational data models, Prisma Client singleton, and first migration.
  - `prisma/models/project.prisma` (new) — multi-file schema model, picked up automatically since `prisma7.config.ts` already points `schema` at the `prisma/` directory rather than a single file.
    - `Project`: `id` (`cuid()`), `ownerId` (Clerk user ID, stored as a plain string — no FK, since users live in Clerk, not this database), `name`, optional `description`, `status` (`ProjectStatus` enum: `DRAFT` | `ARCHIVED`, `@default(DRAFT)`), `canvasJsonPath` (optional — no snapshot exists until the canvas subsystem lands), `createdAt`/`updatedAt`. Indexes on `ownerId` and `createdAt` (separate indexes, per spec's "indexes on owner ID and creation date" reading — contrast with `ProjectCollaborator`'s combined `project/date` index below). Reverse `collaborators ProjectCollaborator[]` relation field added because Prisma requires both sides of an explicit relation to be declared.
    - `ProjectCollaborator`: `id` (`cuid()`), `projectId` + `project` relation (`onDelete: Cascade`), `email`, `createdAt`. `@@unique([projectId, email])`; indexes on `email` alone and on the combined `[projectId, createdAt]`, per spec's "project/date" phrasing.
  - `lib/prisma.ts` (new) — cached singleton, following the pattern in `constructor.md`/`Singleton Pattern` (`globalForPrisma` cast on `globalThis`, cached outside production so Next.js hot reloads reuse one client). Branches on `DATABASE_URL`: a `prisma+postgres://` prefix passes `accelerateUrl` straight to the `PrismaClient` constructor (no `@prisma/extension-accelerate` — that package isn't in the spec's "already installed" list, and `accelerateUrl` alone is a valid constructor option without it); anything else (the actual current `DATABASE_URL`, a `postgres://...pooled.db.prisma.io...` pooled connection string) builds a `PrismaPg` adapter from `@prisma/adapter-pg` and passes that instead.
  - Import path note: the `prisma-client` generator's output (`app/generated/prisma/`, per the pre-existing `generator client` block in `prisma/schema.prisma`) has no root `index`/`package.json` re-export — `PrismaClient` must be imported from the `client` subpath specifically (`@/app/generated/prisma/client`), not the bare `@/app/generated/prisma` directory shown in generic Prisma docs/skill examples.
  - Ran `prisma migrate dev --name init_project_models` (applied cleanly, first migration in `prisma/migrations/`) then `prisma generate` explicitly per this Prisma version's CLI behavior (generation is no longer bundled into `migrate dev`).
  - Verified: `prisma validate`, `tsc --noEmit`, `eslint`, and `npm run build` all pass clean.

- `06-project-apis.md`: backend-only REST routes for projects — no UI wiring, per spec ("do not wire the UI yet").
  - `app/api/projects/route.ts` (new) — `GET` lists the authenticated user's own projects (`where: { ownerId: userId }`, newest first); `POST` creates a project owned by the authenticated user, defaulting a missing/blank `name` to `"Untitled Project"` (trimmed; any non-empty string is used as-is). Creation relies on the schema's own `cuid()` default — no client-supplied or sequential ID.
  - `app/api/projects/[projectId]/route.ts` (new) — `PATCH` renames a project (400 if `name` is missing/blank after trimming — not explicitly in the spec's checklist, but required by `code-standards.md`'s "validate input before any logic runs"); `DELETE` removes it. Both call `requireProjectOwner` first: 404 if the project doesn't exist, 403 if the authenticated user isn't `ownerId`.
  - `lib/api-auth.ts` (new) — `requireUserId()`, shared by every route: resolves the Clerk user ID or returns a ready-to-send `401` response.
  - `lib/project-access.ts` (new) — `requireProjectOwner(projectId, userId)`, shared by the rename/delete routes: loads the project and returns it, or a ready-to-send `404`/`403` response.
  - `lib/api-request.ts` (new) — `readJsonBody()`, tolerates an absent/malformed JSON body (returns `{}`) so callers only branch on the fields they care about.
  - `proxy.ts` (modified) — resolves the open question below. All non-public routes were going through Clerk's middleware-level `auth.protect()`, which for API (non-document) requests responds with a redirect/404 rather than `401` — directly conflicting with `06-project-apis.md`'s explicit "unauthenticated requests return `401`" requirement (verified live: unauthenticated `curl` against the new routes came back `307`/`404` before this change). Fixed by adding an `isApiRoute` matcher (`/api(.*)`, `/trpc(.*)`) that skips `auth.protect()` for API routes; those routes now enforce their own auth via `requireUserId()` and return a proper JSON `401`. Non-API protected routes (e.g. `/editor`) are unaffected — still verified to 307-redirect unauthenticated requests, same as before.
  - Incidental fix, unrelated to this spec: `lib/mock-projects.ts` was missing from the repo entirely (`hooks/use-mock-projects.ts` imported it, but the file was never committed in `f649d0e feat: implement dialogs`, despite `04-project-dialogs.md`'s notes above describing it as done — `tsc --noEmit` failed on `main`/`development` HEAD before any of this session's changes). Recreated it from the existing description (two owned projects, one collaborator) so the project actually type-checks and builds; this was necessary to verify the new API routes compile cleanly; no behavior beyond what was already documented was invented.
  - Verified: `tsc --noEmit`, `eslint`, and `npm run build` all pass clean. Live-tested with `next start` (not `next dev` — see Session Notes) against all four routes unauthenticated: all four now return `401` with a JSON `{ "error": "Unauthorized" }` body (previously `307`/`404`); `/editor` still 307s unauthenticated, confirming the `proxy.ts` change didn't loosen page-route protection.

- `07-wire-editor-home.md`: editor home and the project dialogs now run against the real `/api/projects` routes — mock data is gone.
  - `lib/get-projects.ts` (new) — the server-side "project data helper" the spec calls for (none existed before this unit). `getUserProjects(userId, email)` returns `{ owned, shared }`: owned via `prisma.project.findMany({ where: { ownerId } })`, shared via `prisma.projectCollaborator.findMany({ where: { email } })` (collaborators are matched by email, not Clerk user ID — there's no collaborator-invite flow yet, so the shared list is empty in practice until one exists). `getProjectAccess(projectId, userId, email)` resolves one project and verifies the viewer is the owner or a collaborator, for the workspace route below. Both map the Prisma `Project` row into `types/project.ts`'s `Project` shape, deriving `slug` via `slugify(name)` — `slug` is a display-only convenience, not a stored or unique identifier (see the room ID decision below).
  - `app/editor/page.tsx` (rewritten) — now a server component (previously just rendered `<EditorShell />` with no data). Resolves the Clerk user and email (`auth()` + `currentUser()`), calls `getUserProjects`, and passes `ownedProjects`/`sharedProjects` into `EditorShell` as props. No client-side fetch for the initial load, per spec.
  - `app/editor/[projectId]/page.tsx` (new) — the "workspace" `useProjectActions`'s Create flow navigates to once a project exists. Not asked for verbatim in the spec, but required for it: "create navigates to workspace" is an explicit Check-When-Done item, and without a route to land on that navigation 404s. Calls `getProjectAccess`; 404s (`notFound()`) if the project doesn't exist or the viewer isn't the owner/a collaborator, rather than leaking which case it was. Passes the same owned/shared lists (so the sidebar still populates) plus `activeProject` into `EditorShell`.
  - `components/editor/editor-shell.tsx` (rewritten) — now takes `ownedProjects`/`sharedProjects`/`activeProject?` as props instead of owning mock state itself; merges owned+shared into one list for `ProjectSidebar` (which already splits by `role` internally). When `activeProject` is set, the canvas area shows a minimal named placeholder ("{name} — Canvas workspace coming soon") instead of the "create a project" `EditorHome` state — still just a placeholder, since the real canvas subsystem isn't built.
  - `hooks/use-project-actions.ts` (new) — replaces both `hooks/use-mock-projects.ts` and `hooks/use-project-dialogs.ts` (both deleted), per the spec's single named hook ("`Use Project Actions`... manages dialog state and project mutations") — a deliberate departure from the two-hook split `04-project-dialogs.md` set up and `06-project-apis.md`'s progress notes anticipated keeping; 07 names one hook doing both jobs, so that's what's built. Owns dialog mode/active project/name input/`isSubmitting`/`error`, and now performs real `fetch` calls: `POST /api/projects`, `PATCH /api/projects/[id]`, `DELETE /api/projects/[id]`. It holds no project list itself — after rename it calls `router.refresh()` (re-runs the server component's data fetch), after delete it does the same unless the deleted project is the one currently open (`activeProjectId`, passed in from `EditorShell`), in which case it `router.push("/editor")`s instead. After create it `router.push`es straight to `/editor/{project.id}` using the ID the server returned.
  - Room ID decision (user confirmed, given a real fork against `06-project-apis.md`'s already-shipped contract): `POST /api/projects` was **not** changed to accept a client-supplied ID. The create dialog still generates a cosmetic room-ID preview client-side (`slugify(name)-{shortSuffix}`, suffix generated once when the dialog opens) purely so the field in the Wiring checklist ("create dialog shows room ID preview") has something to show before the project exists — but the value actually used for both the database primary key and the Liveblocks room ID (once Liveblocks lands) is the single `project.id` Prisma generates. "The project ID and Liveblocks room ID should stay aligned" is satisfied by only ever having one identifier, not by making the preview and the final ID match byte-for-byte.
  - `components/editor/project-dialogs.tsx` — prop renamed `dialogs` → `actions` (type `UseProjectActionsResult`), `slug` → `roomId` (now spec-named), and each dialog shows `error` (from a failed fetch) using the existing `text-destructive` token — no dialog markup/layout changed otherwise.
  - Deleted: `lib/mock-projects.ts`, `hooks/use-mock-projects.ts`, `hooks/use-project-dialogs.ts` — dead now that the sidebar renders real data and `use-project-actions.ts` owns dialog+mutation state.
  - Verified: `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; both `/editor` and `/editor/[projectId]` list as real routes in the build output. Live-tested with `next start` (not `next dev`, see Session Notes): unauthenticated `GET /editor` and `GET /editor/abc123` both 307-redirect to `/sign-in?redirect_url=...`, confirming the new dynamic route inherits `proxy.ts`'s default protection without any matcher change.

- `08-editor-workspace-shell.md`: the `/editor/[roomId]` workspace shell, server-side access checks, and the full navbar/sidebar/canvas/AI-sidebar chrome. No real canvas logic, Liveblocks, AI chat, or sharing behavior — chrome and access control only, per spec.
  - `app/editor/[projectId]/page.tsx` → renamed to `app/editor/[roomId]/page.tsx` (route param renamed to match spec naming — the room ID is still just the project's `id`, per the existing "single identifier" architecture decision below, so this is a rename, not a new identifier). Now resolves identity via `getCurrentIdentity()` and renders `AccessDenied` (not `notFound()`) when `getProjectAccess` returns `null` — covering both a non-existent project and one the signed-in user can't access, since that helper already merges those cases.
  - `lib/project-access.ts` (extended, not replaced — `requireProjectOwner` from `06-project-apis.md` is untouched, a distinct owner-only concern for mutation routes) — added the two helpers the spec named:
    - `getCurrentIdentity()` — wraps `auth()` + `currentUser()`, returns `{ userId, email } | null`. Now the single source for identity resolution; both `app/editor/page.tsx` and `app/editor/[roomId]/page.tsx` use it instead of each calling Clerk directly.
    - `checkProjectAccess(projectId, userId, email)` — owner-or-collaborator access check, returns `{ project, role } | null`. `lib/get-projects.ts`'s `getProjectAccess` was refactored to call this internally instead of duplicating the same Prisma queries — root-cause fix per `code-standards.md`, not two copies of the same access-control logic.
  - `components/editor/access-denied.tsx` (new) — centered, lock icon (`h-8 w-8`), short message, link back to `/editor`. Rendered standalone (no navbar/sidebar chrome) in place of the workspace when access is denied.
  - `components/editor/ai-sidebar.tsx` (new) — right-edge slide-over placeholder for future AI chat, mirroring `ProjectSidebar`'s overlay pattern (fixed, `top-14 bottom-0`, slide via `translate-x`) but anchored right and with no tabs — just a header + close button + "AI chat coming soon." empty state, per the `ui-context.md` layout pattern ("slide-over AI sidebar on the right").
  - `components/editor/editor-navbar.tsx` — added optional `projectName` (rendered centered) and `isAiSidebarOpen`/`onToggleAiSidebar` props. The Share button (`Share2` icon, no handler — sharing behavior is explicitly out of scope) and the AI sidebar toggle (`Sparkles` icon, does toggle real open/closed state) both render only when `projectName` is set, i.e. only inside a room — matches the spec's scoping to `/editor/[roomId]`, so plain `/editor` (no active project) keeps its current bare navbar.
  - `components/editor/project-sidebar.tsx` — added `activeProjectId` prop; `ProjectRow` now takes `isActive` and highlights the current room's row (`bg-accent-dim` background, `text-brand` name) so the open project is visually distinguishable in the list.
  - `components/editor/editor-shell.tsx` — lifted `isAiSidebarOpen` state (default closed) alongside the existing `isSidebarOpen`; wires `projectName`/`activeProjectId` down to the navbar and sidebar, and renders `<AiSidebar>` (always mounted, only reachable via the room-scoped toggle button).
  - `app/editor/page.tsx` — swapped its inline `auth()`/`currentUser()` calls for `getCurrentIdentity()`, no behavior change.
  - Verified: `next build` (not `next dev`, see Session Notes) passes clean — `tsc`, `eslint` (0 errors, same pre-existing unrelated warning as before), and the build output lists `/editor` and `/editor/[roomId]` as real routes. `next start` smoke test: unauthenticated `GET /editor` and `GET /editor/nonexistent-room-id` both 307 to `/sign-in`, confirming the identity check runs before any project lookup. `AGENTS.md` confirmed untouched after the build/start run.
  - Follow-up visual pass, from a user-supplied reference screenshot — restyling only, no new functionality (still no Liveblocks, AI chat, or sharing behavior):
    - `editor-navbar.tsx` — left cluster is now a bordered icon toggle (`variant="outline"`) next to a two-line title (project name + "Workspace" subtitle), replacing the old three-column centered layout. Share and AI buttons switched from icon-only ghost buttons to labeled `outline`-variant buttons ("Share", "AI"); the AI button switches to the filled `default` (brand cyan) variant while `isAiSidebarOpen` is true, reusing existing `Button` variants rather than one-off colors.
    - `project-sidebar.tsx` — "My Projects"/"Shared" `TabsList`/`TabsTrigger` restyled into a segmented pill (`rounded-full bg-subtle`, active tab `bg-elevated`) via `className` overrides only (`components/ui/tabs.tsx` itself untouched, per the protected-foundation rule). `ProjectRow` now shows a small brand-colored dot before the active row's name (space reserved on inactive rows too, so text stays aligned) and uses `rounded-xl`/larger padding, matching the radius scale in `ui-context.md`. The bottom "New Project" button gained `rounded-full` to read as a pill.
    - `components/editor/canvas-placeholder.tsx` (new) — extracted from the inline JSX that used to live in `editor-shell.tsx`, per "keep modules small and single-purpose." Renders a ringed `Compass` icon, a "WORKSPACE SHELL" eyebrow, a heading, and a paragraph that explicitly states the canvas/AI/presence work is not built yet ("the shell is wired with project context and navigation only") — worded so the richer placeholder copy can't be mistaken for real functionality.
    - `ai-sidebar.tsx` — rebuilt from a centered one-line empty state into: a header ("AI Copilot" / "Placeholder panel" + a decorative `Sparkles` icon, plus the existing close button, now next to it instead of alone), a "Chat surface pending" card (`Bot` icon in an `bg-ai/15`/`text-ai-text` badge) whose copy states plainly that messaging/generation are out of scope, and a "Future hooks" footer note. Widened `w-80` → `w-96`. Still no chat input, message list, or send behavior — copy and chrome only.
    - Verified: `next build` and `eslint` both pass clean (0 errors, same pre-existing unrelated warning); `AGENTS.md` untouched.

- `09-share-dialog.md`: real Share dialog, collaborator APIs, and Clerk enrichment — no local user table, per spec.
  - `types/collaborator.ts` (new) — `Collaborator` shape returned to the client: `id`, `email`, `displayName: string | null`, `imageUrl: string | null` (both `null` when no Clerk user matches the email).
  - `lib/clerk-users.ts` (new) — `getClerkUsersByEmail(emails)`: calls `clerkClient().users.getUserList({ emailAddress: [...] })` (the Backend API — `@clerk/nextjs/server`'s `clerkClient()`, confirmed via its `.d.ts` before use per `AGENTS.md`) and returns a map keyed by lowercase email to `{ displayName, imageUrl }`. Display name is `firstName + lastName`, falling back to `username`, falling back to `null` — the UI falls back further to the bare email per spec ("fall back to showing the email only").
  - `lib/collaborators.ts` (new) — `listCollaborators(projectId)`: loads `ProjectCollaborator` rows and merges in the Clerk enrichment map. Shared by the GET/POST/DELETE routes below so every response returns the same enriched shape, and so the Clerk lookup logic exists in exactly one place per `code-standards.md`.
  - `app/api/projects/[projectId]/collaborators/route.ts` (new) — `GET` lists collaborators for anyone with access to the project (owner or collaborator, via the existing `checkProjectAccess`) — read-only per spec ("Collaborators can view the collaborator list"). `POST` invites by email, owner-only (`requireProjectOwner`); validates the email with a simple pattern before touching the database, and returns `409` on a duplicate invite (Prisma `P2002` on the existing `@@unique([projectId, email])`) rather than a generic 500.
  - `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (new) — `DELETE` removes a collaborator, owner-only; 404s if the collaborator doesn't exist or belongs to a different project (scoping check, not just an ID lookup).
  - `hooks/use-share-dialog.ts` (new) — owns the dialog's open state, the fetched collaborator list, the invite-email input, remove-in-flight state (per-row, via `removingId`), and the transient `isCopied` copy-link feedback (auto-clears after 2s). The share link itself is computed in `open()` (not a `useEffect`) from `window.location.origin` — an effect that calls `setState` synchronously on every mount was flagged by `eslint`'s `react-hooks/set-state-in-effect` rule, and computing it at the point the user actually opens the dialog is both simpler and avoids the lint error entirely.
  - `components/editor/share-dialog.tsx` (new) — built on the existing `components/ui/dialog.tsx` (unmodified, per the protected-foundation rule) plus the newly-added `components/ui/avatar.tsx` (see below). Copy-link row (readonly `Input` + `Copy`/`Copied!` button, per spec's "temporary `Copied!` feedback"); invite-by-email form and per-row remove buttons render only when `isOwner`; the collaborator list (avatar with Clerk image or an initial-letter `AvatarFallback`, display name with the email as a secondary line when a name is known, otherwise just the email) renders identically for both roles, satisfying "collaborators... view the collaborator list only."
  - `components/ui/avatar.tsx` (new, shadcn CLI — `npx shadcn add avatar`, per `ui-context.md`'s "use the shadcn CLI to add new components rather than writing them from scratch"; base-nova style on `@base-ui/react/avatar`, matching the existing `button`/`dialog`/`tabs` components already in the project).
  - `components/editor/editor-navbar.tsx` — added an `onShare` prop, wired to the existing Share button's `onClick` (previously present but handler-less, per `08-editor-workspace-shell.md`'s note that sharing was out of scope for that unit).
  - `components/editor/editor-shell.tsx` — added `useShareDialog({ projectId: activeProject?.id, isOwner: activeProject?.role === "owner" })` and renders `<ShareDialog>` alongside the existing `<ProjectDialogs>`, only when a project is active (mirrors the Share button's own visibility rule in the navbar). `activeProject.role` (already resolved server-side by `checkProjectAccess` in `lib/project-access.ts`, per `08-editor-workspace-shell.md`) is the single source for the owner/collaborator gate — no separate ownership check was added client-side.
  - Verified: `tsc --noEmit`, `eslint` (0 errors across all new/changed files), and `npm run build` all pass clean — the build output lists the two new collaborator routes. `next start` smoke-tested: unauthenticated `GET`/`POST`/`DELETE` against `/api/projects/nonexistent/collaborators(/nonexistent)` all return `401`, consistent with every other route under `/api/projects`. `AGENTS.md` confirmed untouched after the build/start run.
  - Follow-up visual pass, from a user-supplied reference screenshot — restyling only, no behavior change (list contents, ownership gating, and API calls are all untouched):
    - `components/editor/share-dialog.tsx` — reworked into the reference's card layout: a bordered `rounded-2xl` "Workspace link" panel (label + helper text + a pill `Copy link` button using the existing `secondary` Button variant, which already tokens to `--bg-subtle`/`--text-primary` — no hand-rolled colors needed) replaces the old plain input-plus-button row and no longer displays the raw URL text, since the reference doesn't either (the spec's "copy the project link with temporary Copied! feedback" only requires the action + feedback, not a visible input). The invite email field gained an inline `Mail` icon and `teammate@company.com` placeholder; the invite button switched to a `rounded-full` pill. The collaborator list moved into one bordered `rounded-2xl` container with `divide-y` rows, each showing a small muted "Collaborator" pill badge for visual parity with the reference's role badge — no owner row was added to the list (the reference shows one because its owner is also a `ProjectCollaborator`-equivalent entry in their data; this app's list is intentionally collaborators-only per the spec's "view current collaborators," and inventing an owner-lookup path wasn't asked for here, so it was left out per `ai-workflow-rules.md`'s "do not invent product behavior not defined in the context files"). Removed the explicit footer `Close` button — `components/ui/dialog.tsx`'s built-in top-right close icon (already present, untouched) is the reference's only visible close affordance, and `components/ui/dialog.tsx` itself was still not modified, per the protected-foundation rule.
    - `components/editor/editor-shell.tsx` — dropped the now-unused `projectName` prop from the `ShareDialog` call site (the dialog title is now the reference's generic "Share project," not project-name-specific).
    - Verified: `tsc --noEmit`, `eslint`, and `npm run build` all pass clean; `AGENTS.md` confirmed untouched.
  - Code-review fix (CodeRabbit, on `project-sidebar.tsx`): sidebar project rows were plain non-interactive `div`s — there was no way to open a project by clicking its row, and shared/collaborator rows had no action buttons at all, so shared projects couldn't be opened from the sidebar by any means. This was a real gap against `04-project-dialogs.md`'s own home-screen copy ("choose a project from the sidebar"), not new scope. `components/editor/project-sidebar.tsx`'s `ProjectRow` now wraps just the dot+name label in a `next/link` `Link` to `/editor/${project.id}`, kept as a sibling of (not a parent of) the rename/delete `Button`s — avoids nesting interactive elements inside an anchor, and keeps "click row to open" and "click action icon to rename/delete" as separate targets, applying to both owned and shared rows since they share the same component. Verified: `tsc --noEmit`, `eslint`, `npm run build` clean; `AGENTS.md` untouched.
  - Code-review fix (CodeRabbit, on `ai-sidebar.tsx`): the closed/off-screen `AiSidebar` and `ProjectSidebar` overlays were marked `aria-hidden` when closed, but `aria-hidden` alone doesn't remove an element from keyboard tab order — a keyboard user could still `Tab` into the closed sidebar's close button (and, for `ProjectSidebar`, its tabs/row actions/New Project button) while it sat translated off-screen. Fixed by adding the `inert` boolean attribute (supported directly as a JSX prop as of React 19, already in use here) alongside the existing `aria-hidden`, on both `components/editor/ai-sidebar.tsx`'s and `components/editor/project-sidebar.tsx`'s `<aside>` — `inert={!isOpen}` makes every focusable descendant genuinely unfocusable and un-clickable while closed, not just hidden from assistive tech. Applied to both overlay sidebars since they share the identical open/closed pattern (per `08-editor-workspace-shell.md`'s "mirrors `ProjectSidebar`'s floating overlay pattern"), not just the one CodeRabbit flagged. Verified: `tsc --noEmit` (confirms `@types/react@19`'s DOM types already accept `inert`), `eslint`, `npm run build` clean; `AGENTS.md` untouched.
  - Code-review fix (CodeRabbit, on `share-dialog.tsx`): the "Workspace link" card (copy-link button + Clerk-driven feedback) rendered unconditionally, so a collaborator opening the Share dialog could copy the project link — a real spec violation, since `09-share-dialog.md` scopes "copy the project link" to the Owners list and states collaborators may only "view the collaborator list" (nothing else). Wrapped that block in `{isOwner && (...)}`, matching the existing gating already applied to the invite form and the per-row remove buttons. Also restored the owner/collaborator split in the dialog's description text (owners see the full "invite/copy/manage" copy; collaborators see "People with access to this project.") — this had quietly regressed to one unconditional string during the visual restyle pass. Verified: `tsc --noEmit`, `eslint`, `npm run build` clean; `AGENTS.md` untouched.

## In Progress

- None — all completed work above is done as scoped.

## Next Up

- Real canvas subsystem (Liveblocks + React Flow) — no feature-spec file yet. `app/editor/[roomId]/page.tsx` currently renders only a named placeholder; this is where the real room/canvas would mount. The AI chat sidebar is still a chrome-only placeholder from `08-editor-workspace-shell.md` — no Liveblocks room or AI chat wired yet. Sharing is now real (`09-share-dialog.md`).
- The "Shared" tab and `getUserProjects`'s shared list can now actually populate — `09-share-dialog.md`'s invite flow writes real `ProjectCollaborator` rows — but Liveblocks room tokens still aren't issued anywhere (`architecture-context.md`'s "Liveblocks room tokens are issued only after verifying project membership" has no code path yet, since there's no Liveblocks integration).

## Open Questions

- None open — the `createRouteMatcher`/`auth.protect()` question below was resolved while implementing `06-project-apis.md` (see above): `proxy.ts` now excludes `/api` routes from middleware-level `auth.protect()`, since those routes enforce auth themselves. `createRouteMatcher` remains in use (and still `@deprecated` in this Clerk version) for the page-route split between public and protected routes; revisit if that split grows more complex.

## Architecture Decisions

- Route protection lives in `proxy.ts` at the project root, not `middleware.ts` — Next.js 16 renamed the file convention (`middleware` → `proxy`); the API and semantics are otherwise unchanged.
- Clerk `appearance` is themed once, centrally, in `lib/clerk-appearance.ts` (`{ theme: dark, variables: {...} }` using this Clerk version's `theme` key, not the older `baseTheme` prop) and passed to `ClerkProvider` in the root layout, rather than repeated per-component — all Clerk UI (`SignIn`, `SignUp`, `UserButton`) inherits it automatically.
- Project data flows server → client one-way: `app/editor/page.tsx` and `app/editor/[roomId]/page.tsx` (renamed from `[projectId]` in `08-editor-workspace-shell.md`) fetch via `lib/get-projects.ts` and pass props down; `hooks/use-project-actions.ts` never holds its own copy of the project list, it triggers `router.refresh()`/`router.push()` after a mutation and lets the server component re-fetch. This replaces the earlier two-hook mock-data split (`useMockProjects` + `useProjectDialogs`) noted in prior sessions — superseded by `07-wire-editor-home.md`.
- A project's database `id` (Prisma `cuid()`) is the single identifier used everywhere, including as the future Liveblocks room ID — there is no separate stored "room ID" field. The create dialog's room-ID preview is cosmetic (client-generated slug+suffix) and does not become the real ID; see `07-wire-editor-home.md` notes above for why.
- `proxy.ts`'s middleware-level `auth.protect()` now applies only to page routes, not `/api`/`/trpc`. API routes enforce auth per-route instead (`lib/api-auth.ts`'s `requireUserId()`), because Clerk's `auth.protect()` doesn't return `401` for unauthenticated non-document (API) requests — it redirects or 404s. Any new `app/api/*` route must call `requireUserId()` itself; it will not get auth protection from `proxy.ts`.
- The page-background color token is exposed to Tailwind as `bg-canvas`, not `bg-base` (its CSS variable is still `--bg-base`). Registering it as `--color-base` collided with Tailwind's own font-size scale key `base` (`text-base` = 1rem); a color theme value wins that collision, so every `text-base` in the app (the create/rename project dialog inputs, `DialogTitle`, `Card` title, `Textarea`, the auth layout headline) was silently rendered as `color: var(--bg-base)` — near-black text on a near-black card — instead of getting a font size. See the reserved-name note in `context/ui-context.md`.
- Collaborators are identified by email only (`ProjectCollaborator.email`), never by Clerk user ID — there's still no invite flow that resolves an email to a Clerk account at invite time, only at *display* time (`lib/clerk-users.ts`, `09-share-dialog.md`). This means inviting an email with no Clerk account is allowed (it just displays as email-only in the Share dialog and never gains project access via Clerk auth, since `checkProjectAccess` matches on the signed-in user's own email). Clerk's `getUserList({ emailAddress: [...] })` filter is an exact match (distinct from `UserCountParams`'s documented partial-match behavior for the same field name) — verified via its `.d.ts` before relying on it.

## Session Notes

- This app runs on pre-release Next.js 16 and a very new `@clerk/nextjs@7.8.3`/`@clerk/ui@1.31.0` — both diverge from older training-data conventions (`proxy.ts` instead of `middleware.ts`; Clerk's `theme` appearance key instead of `baseTheme`; `createRouteMatcher` deprecated). Per `AGENTS.md`, check `node_modules/next/dist/docs/` and the installed package's own `.d.ts`/dist files before assuming an API shape here.
- `/editor` now exists (chrome only, no real canvas) — the post-login 404 the user hit is fixed.
- Confirmed (known repo quirk, not triggered here): `next dev` — not `next build`/`next start`, both of which were used to verify this unit — silently rewrites `AGENTS.md` with an injected instruction nudging a commit. Verified `AGENTS.md` was untouched (`git status`) after this session's `npm run build` and `next start` runs.
